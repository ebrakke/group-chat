{
  "name": "relay-chat-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp src/index.html src/style.css src/app.js src/manifest.json src/sw.js src/icon-*.png dist/",
    "dev": "bun run build && echo 'Built to dist/'"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
