{
  "name": "relay-chat-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build.js",
    "dev": "bun run build && echo 'Built to dist/'"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
