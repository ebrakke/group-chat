{
  "name": "relay-chat-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp src/index.html dist/ && cp src/style.css dist/ && cp src/app.js dist/",
    "dev": "bun run build && echo 'Built to dist/'"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
