<script lang="ts">
  // The root layout handles all routing logic (bootstrap/login/channels redirects).
  // This page just shows a loading state while the layout decides where to navigate.
</script>

<div class="flex items-center justify-center h-screen font-mono"
     style="background: var(--background); color: var(--rc-timestamp);">
  <p class="text-[13px]">loading...</p>
</div>
